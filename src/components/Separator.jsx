import * as Separator from '@radix-ui/react-separator';

const SeparatorComponent = () => {
	return <Separator.Root className='h-[0.7px] w-full bg-[#C7C7C7]' />;
};

export default SeparatorComponent;
